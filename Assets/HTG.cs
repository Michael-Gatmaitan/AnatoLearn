using UnityEngine;

public class HTG : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    // public HandTrackingGraph handTrackingGraph;

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
